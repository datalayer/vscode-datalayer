# webview/utils/ - Webview Utilities

Utility functions specific to the webview context.

## Files

- **utils.ts** - Core webview utilities including `loadFromBytes()` and `saveToBytes()` for notebook data serialization/deserialization between the extension and webview, and `getNonce()` for CSP support in dynamically created content.
