#!/usr/bin/env sh
# Conventional commit check - warns but does not block
npx --no -- commitlint --edit "$1" || echo "Warning: commit message does not follow conventional format (feat:/fix:/docs:/etc). Consider amending."
