# src/ui/styles/ - Theme Style Mappings

CSS generation for mapping VS Code theme variables to component library variables.

## Files

- **primerVSCodeTheme.ts** - Returns CSS that maps VS Code CSS variables to Primer React variables using `!important` flags to ensure Primer components match the active VS Code theme.
