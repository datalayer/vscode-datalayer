# Test: Tools

Tests for the LLM tool schema definitions and their validation logic.

## Files

- `toolSchemas.test.ts` - Basic validation tests for all tool parameter schemas (selectKernel, startRuntime, etc.).
- `toolSchemas.extended.test.ts` - Extended tests covering edge cases, boundary values, and negative inputs for tool schemas.
