# src/constants/ - Shared Constants

Extension-wide constant values shared across modules.

## Files

- **kernelConstants.ts** - Shared constants for kernel identification and management, including `LOCAL_KERNEL_URL_PREFIX` for local kernel connections and utility function `isLocalKernelUrl()`.
